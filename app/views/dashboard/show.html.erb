<h1>You did it!</h1>
<%= current_user.name %>
<%= current_user.email %>
<%= image_tag(current_user.profile_picture) %>
<%= link_to "Logout", logout_path, method: :delete %>

<% @activities.list_athlete_activities.each do |activity| %>
  <%= activity["name"] %>
  <%#= binding.pry %>
  <%= @activities.total_elevation_gain(activity) %>
<% end %> 
