<%= render partial: "/navbar/navbar" %> 
Monthly Stats
<%= @stats.sessions("recent") %> sessions
<%= @stats.distance("recent") %> miles
<%= @stats.duration("recent") %>
<%= @stats.elevation_gain("recent") %> feet

Yearly Stats

<%= @stats.sessions("ytd") %> sessions
<%= @stats.distance("ytd") %> miles
<%= @stats.duration("ytd") %>
<%= @stats.elevation_gain("ytd") %> feet

<div class="row">
  <div class="col s12 m6">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <div class="card-title">
          <%= image_tag(current_user.profile_picture, class: "circle profile-pic") %>
        </div>
        <div class="dashboard-large"><%= current_user.name %></div>
        <span><%= current_user.city %>,  <%= current_user.state %></span>
      </div>
    </div>

  <div class="col s12 m6">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <h5 class="center dashboard-large">Upcoming Races/Goals</h5>
        <p id="dashboard-name"><%= current_user.name %></p>
        <p><%= current_user.city %>,  <%= current_user.state %></p>
      </div>
    </div>
  </div>
</div>

<div class="row">
  <div class="col s12 m6">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <div class="card-title">
          <%= image_tag(current_user.profile_picture, class: "circle profile-pic") %>
        </div>
        <div class="dashboard-large"><%= current_user.name %></div>
        <span><%= current_user.city %>,  <%= current_user.state %></span>
      </div>
    </div>
  </div>

  <div class="col s12 m6">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <h5 class="center dashboard-large">Upcoming Races/Goals</h5>
        <p id="dashboard-name"><%= current_user.name %></p>
        <p><%= current_user.city %>,  <%= current_user.state %></p>
      </div>
    </div>
  </div>
</div>


